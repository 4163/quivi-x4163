diff --git "C:\\Users\\x4163\\Desktop\\Programs\\quivi\\backups\\PROJECT_NOTES.md.20260702-203103.bak" "C:\\Users\\x4163\\Desktop\\Programs\\quivi\\PROJECT_NOTES.md"
index f2822d5..b57e1c4 100644
--- "C:\\Users\\x4163\\Desktop\\Programs\\quivi\\backups\\PROJECT_NOTES.md.20260702-203103.bak"
+++ "C:\\Users\\x4163\\Desktop\\Programs\\quivi\\PROJECT_NOTES.md"
@@ -4,6 +4,8 @@
 
 This folder is a patched standalone Quivi build. The work focused on image format support and animated playback while keeping the original portable-folder style intact.
 
+The project changes in this folder were generated by OpenAI's Codex using GPT 5.5 at a Medium Reasoning level.
+
 Implemented:
 
 - Static `.webp` support through FreeImage.
@@ -114,6 +116,10 @@ The `diffs/` folder contains detailed change notes and hashes for the patch hist
 
 `animation-scale-cache.diff` is historical only and was reverted by `revert-animation-scale-cache.diff`.
 
+## Next Steps
+
+The next technical step is to fix and optimize the resampling behavior used by the scaling methods. Bicubic remains the default, but large animated images can still become expensive when high-quality scaling is active. Future work should preserve the current view controls while reducing the cost of Bicubic/Lanczos resizing, especially after zoom or fit changes.
+
 ## Build/Development Notes
 
 Most source-level work was performed against extracted/decompiled source mirrors under `.codex_tmp/src`, then compiled back into Python 2.6 bytecode and packed into `library.zip`.
@@ -125,17 +131,17 @@ Common helper scripts retained under `.codex_tmp` include:
 - small synthetic GIF/WebP test assets.
 - source mirrors used to rebuild the current patched `library.zip`.
 
-Do not ship `.codex_tmp` as part of a minimal end-user release unless you want to include development/debug material.
+`.codex_tmp` is useful development/debug material and can be included for repository history. To reduce storage cost while keeping it easy for another coding agent to inspect, keep it as a standard ZIP archive beside the project files. ZIP is preferred here because it is built into Windows PowerShell and can be expanded quickly with `Expand-Archive`.
 
-## Public Release Hygiene
+## Public Repository Hygiene
 
-Before publishing:
+Before publishing the repository:
 
 - Keep the runtime files in the root folder.
 - Keep `webp_extract_pillow_runtime/`.
 - Keep relevant license files.
-- Exclude `.codex_tmp/` if only distributing the app.
-- Exclude `backups/` and `diffs/` unless publishing the patch history.
+- Include `.codex_tmp.zip` if preserving the development/debug material.
+- Keep `backups/` and `diffs/` if publishing the patch history.
 - Do not include local logs.
 
 Generated logs and private test media were removed during cleanup.
